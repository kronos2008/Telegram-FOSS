workflows:
  telegram-foss-workflow:
    name: Build Telegram-FOSS APK
    max_build_duration: 60
    environment:
      vars:
        CM_BUILD_NUMBER: $BUILD_NUMBER
    scripts:
      # Устанавливаем всё необходимое
      - name: Install tools
        script: |
          brew install ninja cmake go

      # Устанавливаем Java 11 для sdkmanager
      - name: Setup Java 11
        script: |
          brew install openjdk@11
          export JAVA_HOME=/opt/homebrew/opt/openjdk@11/libexec/openjdk.jdk/Contents/Home
          echo 'export JAVA_HOME=/opt/homebrew/opt/openjdk@11/libexec/openjdk.jdk/Contents/Home' >> ~/.zshrc
          source ~/.zshrc

      # Скачиваем нужную версию NDK (с Java 11)
      - name: Setup NDK
        script: |
          export JAVA_HOME=/opt/homebrew/opt/openjdk@11/libexec/openjdk.jdk/Contents/Home
          echo "y" | $ANDROID_HOME/tools/bin/sdkmanager "ndk;21.4.7075529"

      # Сборка нативных зависимостей
      - name: Build native libs
        script: |
          cd TMessagesProj/jni
          export NDK=$ANDROID_HOME/ndk/21.4.7075529
          export NINJA_PATH=/usr/local/bin/ninja
          chmod +x *.sh
          ./build_libvpx_clang.sh
          ./build_ffmpeg_clang.sh
          ./patch_ffmpeg.sh
          ./patch_boringssl.sh
          ./build_boringssl.sh
          cd ../../

      # Сборка APK
      - name: Build APK
        script: |
          chmod +x gradlew
          ./gradlew clean
          ./gradlew assembleAfatRelease

    artifacts:
      - TMessagesProj/build/outputs/apk/afat/release/*.apk

    publishing:
      email:
        recipients:
          - washingtonfizcl@gmail.com
